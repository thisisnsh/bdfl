'use strict'; const test = require('node:test'); const assert = require('node:assert/strict'); const { planView, dependencyView } = require('../../src/tui/plan-view');
const manifest = { title: 'Recovery', version: 1, shared: { sha: 's' }, globalValidation: { sha: 'g' }, approvals: {}, chunks: [{ id: 'foundation', sha: '1', dependsOn: [], locks: [], paths: ['a'] }, { id: 'api', sha: '2', dependsOn: ['foundation'], locks: [], paths: ['b'] }, { id: 'ui', sha: '3', dependsOn: ['foundation'], locks: [], paths: ['c'] }, { id: 'migration', sha: '4', dependsOn: ['foundation'], locks: ['database'], paths: ['d'] }] };
test('renders clean plan state and native dependency waves without markers', () => { assert.doesNotMatch(planView(manifest, 2), /bdfl-/); assert.match(dependencyView(manifest, 2), /Wave 1: foundation/); assert.match(dependencyView(manifest, 2), /Wave 2: api \+ ui/); assert.match(dependencyView(manifest, 2), /Wave 3: migration/); assert.match(dependencyView(manifest, 2), /foundation → api/); });
