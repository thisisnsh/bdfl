{
  "name": "bdfl-agent-orchestrator",
  "version": "0.0.0-development",
  "description": "Benevolent Delegator for LLMs: explicit managed agents for Claude Code and Codex",
  "license": "MIT",
  "author": "thisisnsh",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thisisnsh/bdfl.git"
  },
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "bin": { "bdfl": "bin/bdfl" },
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "package": "node scripts/package.js",
    "package:check": "node scripts/package.js --check",
    "release:version": "node scripts/set-release-version.js",
    "validate": "npm run package:check && node scripts/validate-manifests.js"
  },
  "files": ["bin/", "plugins/", "src/", "install.sh", "install.ps1", "uninstall.sh", "uninstall.ps1", "LICENSE", "README.md"]
}
