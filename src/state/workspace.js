'use strict';

const fs = require('node:fs'); const path = require('node:path'); const crypto = require('node:crypto');
const { atomicWrite } = require('../core/plans'); const { validateProfile, validateWorkstreamConfig } = require('../core/profiles');

const WORKSPACE_SCHEMA = 2;
const RESET_MESSAGE = 'BDFL development state uses an older schema. Stop BDFL, remove this repository\'s .bdfl/ directory, and start again.';
const INVALID_STATE_MESSAGE = 'BDFL durable state contains an invalid record and was not changed';

function defaultWorkspace() { return { schema: WORKSPACE_SCHEMA, workstreams: [], sessions: [], activeWorkstreamId: null, nextPaneNumber: 1 }; }
function printable(value) { return typeof value === 'string' && !/[\u0000-\u001f\u007f-\u009f]/u.test(value); }
function characters(value) { return [...value]; }
function normalizeTaskSnippet(value) {
  if (value === null || value === undefined) return null;
  const normalized = `${value}`.replace(/[\u0000-\u0008\u000b\u000c\u000e-\u001f\u007f-\u009f]/gu, '').replace(/\s+/gu, ' ').trim();
  if (!normalized) return null;
  return characters(normalized).slice(0, 200).join('').trim();
}
function planningProviderName(provider) { if (provider === 'claude') return 'Claude'; if (provider === 'codex') return 'Codex'; const value = `${provider || 'Agent'}`; return value ? `${value[0].toUpperCase()}${value.slice(1)}` : 'Agent'; }
function validateSessionRecord(session) {
  if (!session || !printable(session.name) || session.name !== session.name.trim() || !session.name || characters(session.name).length > 24 || !Number.isInteger(session.roleSequence) || session.roleSequence < 1 || !(session.taskSnippet === null || printable(session.taskSnippet) && characters(session.taskSnippet).length <= 200 && session.taskSnippet === normalizeTaskSnippet(session.taskSnippet))) throw new Error(`Invalid session metadata${session?.id ? ` for ${session.id}` : ''}`);
  if (!['planning', 'direct'].includes(session.sessionType || 'planning') || session.role === 'direct' && session.sessionType !== 'direct') throw new Error(`Invalid session type${session?.id ? ` for ${session.id}` : ''}`);
}

class WorkspaceStore {
  constructor(root, { io = fs, now = () => new Date(), id = () => crypto.randomUUID() } = {}) { this.root = path.resolve(root); this.io = io; this.now = now; this.id = id; this.directory = path.join(this.root, '.bdfl'); this.file = path.join(this.directory, 'workspace.json'); this.configFile = path.join(this.directory, 'config.json'); this.events = path.join(this.directory, 'events.ndjson'); }
  load() { if (!this.io.existsSync(this.file)) return defaultWorkspace(); const value = JSON.parse(this.io.readFileSync(this.file, 'utf8')); if (value.schema !== WORKSPACE_SCHEMA) { const error = new Error(RESET_MESSAGE); error.code = 'RESET_REQUIRED'; throw error; } try { for (const stream of value.workstreams || []) { stream.sessionType ||= 'planning'; validateWorkstreamConfig({ ...stream, version: 1 }); } for (const session of value.sessions || []) { const stream = value.workstreams.find((item) => item.id === session.workstreamId); session.sessionType ||= stream?.sessionType || 'planning'; session.taskSnippet = normalizeTaskSnippet(session.taskSnippet); validateSessionRecord(session); } for (const stream of value.workstreams || []) { const sessions = value.sessions.filter((session) => session.workstreamId === stream.id); const names = sessions.map((session) => session.name.toLocaleLowerCase()); const sequences = sessions.map((session) => `${session.role}:${session.roleSequence}`); if (new Set(names).size !== names.length || new Set(sequences).size !== sequences.length) throw new Error('Duplicate agent metadata'); } } catch (cause) { const error = new Error(`${INVALID_STATE_MESSAGE}: ${cause.message}`, { cause }); error.code = 'STATE_INVALID'; throw error; } return value; }
  loadConfig() { if (!this.io.existsSync(this.configFile)) return null; const value = JSON.parse(this.io.readFileSync(this.configFile, 'utf8')); if (value.schema !== WORKSPACE_SCHEMA) { const error = new Error(RESET_MESSAGE); error.code = 'RESET_REQUIRED'; throw error; } try { const result = { version: 1 }; if (value.profiles?.delegator || value.profiles?.worker || value.workerCapacity !== undefined) Object.assign(result, validateWorkstreamConfig({ version: 1, sessionType: 'planning', delegatorProfile: value.profiles?.delegator, workerProfile: value.profiles?.worker, workerCapacity: value.workerCapacity })); if (value.profiles?.direct) result.directProfile = validateProfile(value.profiles.direct, { worker: true }); return Object.keys(result).length > 1 ? result : null; } catch (cause) { const error = new Error(`${INVALID_STATE_MESSAGE}: ${cause.message}`, { cause }); error.code = 'STATE_INVALID'; throw error; } }
  activateWorkstream(id) { return this.update((state) => { if (!state.workstreams.some((stream) => stream.id === id && stream.status !== 'closed')) throw new Error(`Unknown active session: ${id}`); state.activeWorkstreamId = id; return state; }); }
  save(value) { atomicWrite(this.file, `${JSON.stringify(value, null, 2)}\n`, this.io); return value; }
  update(mutator) { const value = this.load(); return this.save(mutator(structuredClone(value)) || value); }
  event(type, data = {}) { this.io.mkdirSync(this.directory, { recursive: true }); this.io.appendFileSync(this.events, `${JSON.stringify({ id: this.id(), type, at: this.now().toISOString(), ...data })}\n`, { mode: 0o600 }); }
  createWorkstream(config, title = path.basename(this.root)) { const validated = validateWorkstreamConfig(config); let workstream; this.update((state) => { const profile = validated.sessionType === 'direct' ? validated.directProfile : validated.delegatorProfile; const provider = profile.provider; const providerSequence = Math.max(0, ...state.workstreams.map((item, index) => { const candidateProfile = item.sessionType === 'direct' ? item.directProfile : item.delegatorProfile; return candidateProfile?.provider === provider ? item.providerSequence || state.workstreams.slice(0, index + 1).filter((candidate) => (candidate.sessionType === 'direct' ? candidate.directProfile : candidate.delegatorProfile)?.provider === provider).length : 0; })) + 1; const createdAt = this.now().toISOString(); workstream = { id: `workstream-${this.id()}`, title, ...validated, providerSequence, status: 'active', createdAt, updatedAt: createdAt }; state.workstreams.push(workstream); state.activeWorkstreamId = workstream.id; return state; }); let saved = { schema: WORKSPACE_SCHEMA, profiles: {} }; if (this.io.existsSync(this.configFile)) { saved = JSON.parse(this.io.readFileSync(this.configFile, 'utf8')); if (saved.schema !== WORKSPACE_SCHEMA) { const error = new Error(RESET_MESSAGE); error.code = 'RESET_REQUIRED'; throw error; } saved.profiles ||= {}; } if (validated.sessionType === 'direct') saved.profiles.direct = validated.directProfile; else { saved.profiles.delegator = validated.delegatorProfile; saved.profiles.worker = validated.workerProfile; saved.workerCapacity = validated.workerCapacity; } atomicWrite(this.configFile, `${JSON.stringify(saved, null, 2)}\n`, this.io); this.event('workstream.created', { workstreamId: workstream.id, sessionType: validated.sessionType }); return workstream; }
  setCapacity(id, capacity) { if (!Number.isInteger(capacity) || capacity < 1 || capacity > 5) throw new Error('Worker capacity must be an integer from 1 to 5'); let active = 0; const state = this.update((value) => { const stream = value.workstreams.find((item) => item.id === id); if (!stream) throw new Error(`Unknown session: ${id}`); stream.workerCapacity = capacity; stream.updatedAt = this.now().toISOString(); active = value.sessions.filter((session) => session.workstreamId === id && session.role !== 'delegator' && session.status === 'running').length; return value; }); this.event('workstream.capacity', { workstreamId: id, capacity, active }); return { workstream: state.workstreams.find((item) => item.id === id), active, canStart: Math.max(0, capacity - active) }; }
  closeWorkstream(id) { const state = this.update((value) => { const stream = value.workstreams.find((item) => item.id === id); if (!stream) throw new Error(`Unknown workstream: ${id}`); const updatedAt = this.now().toISOString(); stream.status = 'closed'; stream.updatedAt = updatedAt; for (const session of value.sessions.filter((item) => item.workstreamId === id)) { session.status = 'closed'; session.explicitlyClosed = true; session.updatedAt = updatedAt; delete session.pid; } if (value.activeWorkstreamId === id) value.activeWorkstreamId = value.workstreams.find((item) => item.status !== 'closed')?.id || null; return value; }); this.event('workstream.closed', { workstreamId: id }); return state.workstreams.find((item) => item.id === id); }
  reopenWorkstream(id) { const state = this.update((value) => { const index = value.workstreams.findIndex((item) => item.id === id); if (index < 0) throw new Error(`Unknown workstream: ${id}`); const [stream] = value.workstreams.splice(index, 1); const updatedAt = this.now().toISOString(); stream.status = 'active'; stream.updatedAt = updatedAt; value.workstreams.push(stream); for (const session of value.sessions.filter((item) => item.workstreamId === id)) { session.explicitlyClosed = false; session.updatedAt = updatedAt; } value.activeWorkstreamId = id; return value; }); this.event('workstream.reopened', { workstreamId: id }); return state.workstreams.find((item) => item.id === id); }
  deleteWorkstream(id) { const state = this.update((value) => { if (!value.workstreams.some((item) => item.id === id)) throw new Error(`Unknown workstream: ${id}`); value.workstreams = value.workstreams.filter((item) => item.id !== id); value.sessions = value.sessions.filter((item) => item.workstreamId !== id); if (value.activeWorkstreamId === id) value.activeWorkstreamId = value.workstreams.find((item) => item.status !== 'closed')?.id || null; return value; }); this.event('workstream.deleted', { workstreamId: id }); return state; }
  deleteAllWorkstreams() { let workstreams = 0; let sessions = 0; this.update((value) => { workstreams = value.workstreams.length; sessions = value.sessions.length; value.workstreams = []; value.sessions = []; value.activeWorkstreamId = null; value.nextPaneNumber = 1; return value; }); this.event('workstreams.deleted', { workstreams, sessions }); return { workstreams, sessions }; }
  createSession(workstreamId, role, profile, fields = {}) { let session; this.update((state) => { const stream = state.workstreams.find((item) => item.id === workstreamId); if (!stream) throw new Error(`Unknown workstream: ${workstreamId}`); if (stream.sessionType === 'direct' && role !== 'direct') throw new Error('Direct workstreams require a direct primary session'); if (stream.sessionType !== 'direct' && role === 'direct') throw new Error('Direct sessions require a direct workstream'); const siblings = state.sessions.filter((item) => item.workstreamId === workstreamId); const paneNumber = Math.max(0, ...siblings.map((item) => item.paneNumber || 0)) + 1; const roleSequence = role === 'delegator' || role === 'direct' ? stream.providerSequence : Math.max(0, ...siblings.filter((item) => item.role === role).map((item) => item.roleSequence || 0)) + 1; const defaultName = role === 'delegator' || role === 'direct' ? `${planningProviderName(profile?.provider)} ${roleSequence}` : role === 'worker' ? `W ${roleSequence}` : `${planningProviderName(role)} ${roleSequence}`; const createdAt = this.now().toISOString(); session = { ...fields, id: `session-${this.id()}`, workstreamId, sessionType: stream.sessionType || 'planning', role, paneNumber, roleSequence, name: fields.name || defaultName, taskSnippet: normalizeTaskSnippet(fields.taskSnippet), profile: structuredClone(profile), status: fields.status || 'closed', explicitlyClosed: Boolean(fields.explicitlyClosed), createdAt, updatedAt: createdAt }; validateSessionRecord(session); if (siblings.some((item) => item.name.toLocaleLowerCase() === session.name.toLocaleLowerCase())) throw new Error('Agent names must be unique within this session'); stream.updatedAt = session.updatedAt; state.sessions.push(session); state.nextPaneNumber = Math.max(state.nextPaneNumber || 1, paneNumber + 1); return state; }); this.event('session.created', { sessionId: session.id, workstreamId, role, sessionType: session.sessionType }); return session; }
  renameSession(id, name) { if (!printable(name) || name !== name.trim() || !name || characters(name).length > 24) throw new Error('Agent names must be 1–24 printable characters'); const state = this.update((value) => { const session = value.sessions.find((item) => item.id === id); if (!session) throw new Error(`Unknown session: ${id}`); if (value.sessions.some((item) => item.workstreamId === session.workstreamId && item.id !== id && item.name.toLocaleLowerCase() === name.toLocaleLowerCase())) throw new Error('Agent names must be unique within this session'); session.name = name; session.updatedAt = this.now().toISOString(); const stream = value.workstreams.find((item) => item.id === session.workstreamId); if (stream) stream.updatedAt = session.updatedAt; return value; }); this.event('session.renamed', { sessionId: id }); return state.sessions.find((item) => item.id === id); }
  setSessionTaskSnippet(id, input) { const taskSnippet = normalizeTaskSnippet(input); const state = this.update((value) => { const session = value.sessions.find((item) => item.id === id); if (!session) throw new Error(`Unknown session: ${id}`); session.taskSnippet = taskSnippet; session.updatedAt = this.now().toISOString(); const stream = value.workstreams.find((item) => item.id === session.workstreamId); if (stream) stream.updatedAt = session.updatedAt; return value; }); return state.sessions.find((item) => item.id === id); }
  setSessionAttention(id, attention) { const value = this.load(); const session = value.sessions.find((item) => item.id === id); if (!session) throw new Error(`Unknown session: ${id}`); const next = Boolean(attention); if (Boolean(session.attention) === next) return value; session.attention = next; return this.save(value); }
  touchSession(id, conversation = false) { return this.update((state) => { const session = state.sessions.find((item) => item.id === id); if (!session) throw new Error(`Unknown session: ${id}`); const updatedAt = this.now().toISOString(); session.updatedAt = updatedAt; if (conversation) session.conversationAt = updatedAt; const stream = state.workstreams.find((item) => item.id === session.workstreamId); if (stream) stream.updatedAt = updatedAt; return state; }); }
}

module.exports = { WORKSPACE_SCHEMA, RESET_MESSAGE, INVALID_STATE_MESSAGE, WorkspaceStore, defaultWorkspace, normalizeTaskSnippet, planningProviderName };
