# BDFL development

Follow `AGENTS.md`. Claude Code integration uses the directly registered MCP server and installed host hooks. BDFL never replaces native plan approval or silently resolves recovery choices.
