# BDFL development

Follow `AGENTS.md`. Claude Code integration uses the standalone skill under `claude/skills/bdfl` and the directly registered MCP server. BDFL never replaces native plan approval or silently resolves recovery choices.
