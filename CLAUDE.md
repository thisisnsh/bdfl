# BDFL development

Follow `AGENTS.md`. Claude Code integration uses `.claude-plugin/`, the explicit skills under `claude/skills/`, and the status-line hook packaged with the plugin. BDFL never replaces native plan approval or silently resolves recovery choices.
