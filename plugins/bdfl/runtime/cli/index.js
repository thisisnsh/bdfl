'use strict';

const pkg = require('../../package.json'); const { WorkspaceStore } = require('../state/workspace'); const { TerminalSupervisor } = require('../tui/supervisor'); const { checkForUpdates } = require('../core/updates'); const { installFatalErrorHandlers, reportError } = require('../core/errors');
const HELP = `Usage: bdfl [--dangerous|status|help|--version]\n\nRun bdfl with no arguments in a Git workspace to open the foreground terminal supervisor.\nUse --dangerous to bypass provider approvals and sandboxing for every agent in this run.\nCtrl+] toggles BDFL chrome and provider focus. Provider input, arrow keys, and Ctrl+C pass through while content is focused.`;
function status(root) { const value = new WorkspaceStore(root).load(); return `${value.workstreams.length} saved session(s), ${value.sessions.filter((session) => session.status === 'running').length} active agent(s).`; }
function main(argv = process.argv.slice(2), io = process, root = process.cwd(), create = (directory, options) => new TerminalSupervisor(directory, options), updates = checkForUpdates) { void updates(pkg.version); const dangerous = argv.length === 1 && argv[0] === '--dangerous'; const command = dangerous ? null : argv[0]; if (command === '--version' || command === '-v') { io.stdout.write(`${pkg.version}\n`); return 0; } if (command === 'help' || command === '--help' || command === '-h') { io.stdout.write(`${HELP}\n`); return 0; } if (command === 'status') { io.stdout.write(`${status(root)}\n`); return 0; } if (command) { io.stderr.write(`Unknown command: ${command}\n${HELP}\n`); return 1; } if (!io.stdin.isTTY || !io.stdout.isTTY) { io.stderr.write(`${HELP}\n`); return 1; } create(root, { input: io.stdin, output: io.stdout, version: pkg.version, dangerous }).start(); return 0; }
function run(argv = process.argv.slice(2), io = process, root = process.cwd(), create, updates) { try { return main(argv, io, root, create, updates); } catch (error) { reportError(error, io, { version: pkg.version }); return 1; } }
module.exports = { HELP, status, main, run, installFatalErrorHandlers };
