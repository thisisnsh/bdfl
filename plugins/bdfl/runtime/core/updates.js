'use strict';
const https = require('node:https'); const semver = require('semver');
function checkForUpdates(current, { channel = 'latest', timeout = 1200, request = https.get } = {}) { return new Promise((resolve) => { const done = (value) => resolve(value); const call = request({ hostname: 'registry.npmjs.org', path: '/bdfl', headers: { accept: 'application/vnd.npm.install-v1+json' }, timeout }, (response) => { let body = ''; response.setEncoding('utf8'); response.on('data', (chunk) => { body += chunk; }); response.on('end', () => { try { const data = JSON.parse(body); const available = data['dist-tags']?.[channel]; done({ available, current, update: Boolean(available && semver.gt(available, current)), channel }); } catch { done({ offline: true }); } }); }); call.on('timeout', () => { call.destroy(); done({ offline: true }); }); call.on('error', () => done({ offline: true })); }); }
module.exports = { checkForUpdates };
