---
name: list
description: Open BDFL runs, plans, tasks, agents, inbox, and models only when explicitly invoked.
disable-model-invocation: true
---

# List BDFL State

Run `bdfl list` with the Bash tool as an ordinary tool action. Use the interactive interface when a terminal is available and otherwise show the stable text snapshot. Never infer an approval, answer, recovery choice, rewind, cancellation, or integration.
