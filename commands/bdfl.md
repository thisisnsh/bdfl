---
description: Activate and manage BDFL agents
argument-hint: "[list|help|off|provider:model:effort]"
---

Use the packaged BDFL skill and runtime to process `$ARGUMENTS`. Keep the host's native plan mode unchanged. On activation, inspect recovery state before dispatch and render the yellow BDFL status banner.
